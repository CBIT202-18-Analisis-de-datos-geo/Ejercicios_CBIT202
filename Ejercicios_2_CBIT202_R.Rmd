
# Ejercicios N°2: Trabajando con data frames

## Introducción al Análisis de Datos Geoespaciales CBIT202-18

La guía consta de **XX** ejercicios de manejo de datos en formato data frame, y esta diseñada para ser respondida en modo "visual". Recuerde reemplazar todas las partes con '#???' y '???' con su código, y/o trabaje en la zona de 'desarrollo'. **Está evaluación será revisada por pares.**

Realice el desarrollo de estos ejercicios al interior del chunk correspondiente. Si necesita escribir hágalo ocupando #, esto es recomendable para aclarar que hace su código. Puede ayudarse de inteligencia artificial para investigar sobre el funcionamiento de funciones, pero resuelva las preguntas en base a su propio conocimiento e investigación. Recuerde que puede ocupar ? para saber más información sobre las funciones, paquetes instalados y datos contenidos en R base.

```{r}
# Por ejemplo:

# Funcion
?mean()
# Paquete
?stats()
# Datos
?iris()
```

Para acceder a los atajos del teclado puede presionar Alt + Shift + K (en Windows/Linux), Option + Shift + K (en macOS) o Help \> Keyboard Shortcuts Help. No olvide que puede retroceder en cualquier cambio que realice con Ctrl + Z.

## **Ejercicios (XXpts):**


**1)** Instale, si es necesario, y llame el paquete: "tidyverse" ¿Qué significa el aviso que entrega el paquete tidyverse sobre conflictos? ¿Cómo puedo asegurarme de estar llamando la función "filter()" del paquete tidyverse sabiendo que el paquete stats, de R base, contiene una función con el mismo nombre?

Para el data frame 'trees', ocupe la función filter() de tidyverse para saber cuántos árboles poseen una altura mayor a 80 (2pt):

```{r}
# Desarrollo: 





```

**2)** El siguiente código crea y guarda un archivo separado por comas en su computadora, por favor no modifique lo que está antes de la parte de desarrollo. Deberá cargar este archivo desde su computadora a R, para esto ayúdece de la función getwd() para saber la localización de su directorio de trabajo, ya que en este se habrá guardado su archivo. 

Explique que hace el código para crear cada variable del data frame. (2pts)

```{r}
# Creación de un df
df <- data.frame(ID = c(1:1000),
                 arboles = sample(c('roble', 'rauli', 'coigue'), size = 1000, replace = TRUE),
                 DAP = rbeta(1000, shape1 = 2, shape = 5),
                 estado_sanitario = factor(rbinom(1000, size = 1, prob = 0.2), levels = c(0, 1), labels = c('sano', 'enfermo')))

# Guardado del df en su computadora con el nombre df_ejercicio_7
write.csv(df, 'df_ejercicio_7.csv')

# ver working directory
getwd()

# lea el data frame
df_leido <- #???
print(df_leido)

# Desarrollo:




```

**3)** Filtre (filter()), seleccione columnas (select()) y ordene (arrange()) en el siguiente data frame con datos de una Encuesta Social General de EE.UU., de tal manera que devuelva un vector o un valor que responda las siguientes preguntas (apollece ademas en otras funciones como: length(), first(), mean(), etc):
a- ¿En que años nacieron las personas cristianas?
b- ¿Cual es el valor más alto de horas viendo televición en la encuesta? 
c- ¿Cómo se ve un histograma de las edades (hist())?
d- 

```{r}
# install.packages('datos')
# install.packages('tidyverse')
library(datos)
library(tidyverse)

encuesta %>% glimpse()

a <- #???
print(a)



```
**4)** Mencione las 3 características de los datos ordenados y diga por qué el data frame table2 no se encuentra ordenado, en comparación con table1 que si lo esta (2pts):

```{r}
# Data frame ordenado:
print(table1)

# Data frame NO ordenado:
print(table2)

# Desarrollo:


```

### **Bonus**

Ocupe la función pivot_wider para transformar la tabla2 en datos ordenados

```{r}
# Desarrollo:
# data frame no ordenado
table2

# información de pivot_longer
?pivot_longer

# ordenar el data frame
tabla_ordenada <- #???
  
print(tabla_ordenada)
```
